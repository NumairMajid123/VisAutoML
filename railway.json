{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "RAILPACK",
        "buildCommand": "/bin/sh -c 'python3.9 -m pip install --no-cache-dir -r Backend/requirements.txt'"
    },
    "deploy": {
        "startCommand": "/bin/sh -c 'cd Backend && python3.9 manage.py migrate && python3.9 manage.py collectstatic --noinput && gunicorn VisAutoML.wsgi:application'",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}
